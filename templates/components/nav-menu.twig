{% set query = craft.entries().section("pages") %}

{% set entries = query.all() %}

<ul class="nav-menu">
  {# Display the entries #}
  {# {% for entry in entries %}
    <li class="nav-menu-item">
      <a href="{{ entry.url }}">{{ entry.title }}</a>
    </li>
  {% endfor %} #}

  <li class="nav-menu-item close-menu"><a href="{{ siteUrl }}index">index</a></li>
  <li class="nav-menu-item close-menu"><a href="{{ siteUrl }}info">info</a></li>
  <li class="nav-menu-item close-menu"><a href="{{ siteUrl }}journal">journal</a></li>
</ul>
